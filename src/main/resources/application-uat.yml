# UAT profile (SPRING_PROFILES_ACTIVE=uat)
spring:
  datasource:
    clubone:
      url: jdbc:postgresql://clubone-free-tier-db.cju8eqswasju.us-east-1.rds.amazonaws.com:5432/clubone_uat
      username: clubone_dev_user
      password: C1ub0ne@2024!

# UAT API host (parallel to develop-api.clubone.io).
# Payment charge-at-will must hit the gateway (same host as txn), not localhost —
# local payment-service on :8010 is optional and often down / times out.
clubone:
  billing:
    payment:
      http:
        # Must be https — http returns ALB 301 text/html and RestTemplate fails to parse JSON.
        base-url: https://uat.api.clubone.io
        txn-base-url: https://uat.api.clubone.io
  notification:
    base-url: https://uat.api.clubone.io
